@model LawnPaynes.Models.Customer

@{
    ViewBag.Title = "Customer Detail: " + Model.Name;
}

<h2 class="text-center">@Model.Name</h2>

<div class="container col-11 col-md-6 text-white text-center">
    <div class="row justify-content-center bg-success ">
        <div class="col-6">
            <h5>
                @Html.LabelFor(m => m.Name):
            </h5>
        </div>
        <div class="col-6">
            <h5>
                @Model.Name
            </h5>
        </div>
    </div>


    @{
        if (Model.IsNew)
        {
            <div class="row justify-content-center bg-danger ">
                <div class="col-6">
                    <h5>
                        @Html.LabelFor(m => m.IsNew):
                    </h5>
                </div>
                <div class="col-6">
                    <h5>
                        Yes
                    </h5>
                </div>
            </div>
        }
        else
        {

            <div class="row justify-content-center bg-success ">
                <div class="col-6">
                    <h5>
                        @Html.LabelFor(m => m.IsNew):
                    </h5>
                </div>
                <div class="col-6">
                    <h5>
                        No
                    </h5>
                </div>
            </div>
        }
    }

    <div class="row justify-content-center bg-success ">
        <div class="col-6">
            <h5>
                @Html.LabelFor(m => m.PhoneNumber):
            </h5>
        </div>
        <div class="col-6">
            <h5>
                @Model.PhoneNumber
            </h5>
        </div>
    </div>

    @Html.ActionLink("Edit Customer", "Edit", new { id = Model.Id }, new { @class = "btn btn-primary btn-lg margin-right" })
    @Html.ActionLink("Customer List", "Index", null, new { @class = "btn btn-primary btn-lg margin-right" })
